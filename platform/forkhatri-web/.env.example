# Base URL of the platform Identity & Trust Service (docs/ParentApp/07-tech-reqs.md TR13).
# Every call is made with credentials: "include"; the session lives only in an HttpOnly cookie.
NEXT_PUBLIC_IDENTITY_API=http://localhost:8100
